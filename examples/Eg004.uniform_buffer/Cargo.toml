[package]
name = "uniform_buffer"
version = "0.1.0"
authors = ["usami-renko <usami-ssc@protonmail.com>"]
description = "Use uniform buffer in rendering."

[dependencies]
hakurei = { path = "../.." }
cgmath = "0.16.1"
